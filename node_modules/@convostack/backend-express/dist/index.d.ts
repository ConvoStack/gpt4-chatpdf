import * as express from 'express';
import { IStorageEngine, IConversationEventServiceOptions } from '@convostack/models';
import { IAuthProvider } from '@convostack/auth';
import { Config, ExpressContext } from 'apollo-server-express';
import { IAgentManager } from '@convostack/agent';

interface IConvoStackBackendConfiguration {
    basePath?: string;
    storage: IStorageEngine;
    auth: IAuthProvider;
    agents: IAgentManager;
    conversationEventServiceOptions?: IConversationEventServiceOptions;
}
declare class ConvoStackBackendExpress {
    private config;
    private services;
    constructor(config: IConvoStackBackendConfiguration);
    private getCleanBasePath;
    init(app: express.Express, httpServer: any, apolloServerOptions?: Partial<Config<ExpressContext>>): Promise<void>;
}

export { ConvoStackBackendExpress, IConvoStackBackendConfiguration };
