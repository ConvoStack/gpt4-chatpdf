import { IAgent, IAgentContext, IAgentResponse } from '@convostack/agent';
import { BaseChain } from 'langchain/chains';
import { ChainValues } from 'langchain/schema';
import { Callbacks } from 'langchain/callbacks';

declare class LangchainAgentWrapper<T extends BaseChain> implements IAgent {
    private agentOrChain;
    private chainValues;
    private userInputKey;
    private callbacks;
    constructor(agentOrChain: T, chainValues?: ChainValues, userInputKey?: string, callbacks?: Callbacks);
    reply(context: IAgentContext): Promise<IAgentResponse>;
}

export { LangchainAgentWrapper };
