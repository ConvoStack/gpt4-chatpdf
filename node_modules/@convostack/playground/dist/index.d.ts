import { IAgent } from '@convostack/agent';

declare const playground: (agent: IAgent) => void;
declare class PlaygroundAgentProxy {
    private ws;
    private agent;
    private operation;
    private proxyUrl;
    constructor(agent: IAgent);
    setProxyUrl(url: string): void;
    connect(): void;
}

export { PlaygroundAgentProxy, playground };
